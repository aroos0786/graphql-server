
type Query {
    users : [User!]!
    user(id: ID!): User!
    admins: [Admin!]!
    admin(id: ID!): Admin!
}

type User {
    id: ID!
    name: String!
    email: String!
    age: Int
}

type Admin {
    id: ID!
    name: String
    email: String
}